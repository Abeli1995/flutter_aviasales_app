import 'package:flutter/material.dart';

class DifficultRoad extends StatelessWidget {
  const DifficultRoad({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Сложный маршрут'),
      ),
    );
  }
}
